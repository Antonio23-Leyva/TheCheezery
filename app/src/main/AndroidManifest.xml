<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.itson.thecheezery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="luis.leyva.thecheezery.HotDrinks"></activity>
        <activity android:name="luis.leyva.thecheezery.Salties" />
        <activity android:name=".comboGrid" />
        <activity android:name="luis.leyva.thecheezery.productoCombo" />
        <activity android:name="luis.leyva.thecheezery.Combos" />
        <activity android:name="luis.leyva.thecheezery.Postres" />
        <activity android:name="luis.leyva.thecheezery.ProductosActivity" />
        <activity android:name="luis.leyva.thecheezery.MenuActivity" />
        <activity android:name="luis.leyva.thecheezery.RegisterActivity" />
        <activity android:name="luis.leyva.thecheezery.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>